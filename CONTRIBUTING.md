# Contributing

Install the locked dependencies and run the project checks:

~~~bash
npm ci
npm test
npm run build
~~~

Preserve provenance and explicit uncertainty in decision outputs. Add tests for evidence parsing, compiler transforms, exports, and failure states when changing domain logic.

Do not commit private evidence, credentials, or generated operational reports.
